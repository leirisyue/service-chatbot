version: "3.9"

services:
  chatbot:
    build: .
    container_name: rag-chatbot
    env_file:
      - .env
    ports:
      - "8000:8000"
    restart: unless-stopped
    # Nếu cần truy cập host Ollama qua host.docker.internal, thêm:
    extra_hosts:
      - "host.docker.internal:host-gateway"